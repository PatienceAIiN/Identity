<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- OTA updates. This permission lets the app hand a verified APK to the
         system installer (which still asks the user to confirm). It is also
         the strongest signal Play Protect uses against sideloaded apps, so the
         build can drop it: assembleRelease -PotaInApp=false hands updates to
         the browser instead. -->
    <uses-permission android:name="${otaPermission}" />
    <!-- The scanner reads codes on-device; nothing is uploaded. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The camera libraries declare a *required* camera. Everything except the
         scanner works without one, so override it rather than excluding
         camera-less devices. -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false"
        tools:replace="android:required" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"
        tools:replace="android:required" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Identity.Splash">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
